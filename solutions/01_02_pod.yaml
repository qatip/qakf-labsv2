apiVersion: v1
kind: Pod
metadata:
  labels:
    run: hello
  name: hello
spec:
  containers:
  - image: public.ecr.aws/qa-wfl/qa-wfl/qakf/sbe:v1
    name: hello
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
